app:
  host: 0.0.0.0
  port: 8888

vpn_env:
  # my ip endpoint
  ip: https://5jr45codhg.execute-api.us-east-1.amazonaws.com/ip
  # directory of nord ovpn confs
  vpnconfigs: configs
  # download ovpn configs on init
  reload_configs_on_startup: no
  # openvpn conf
  vpnconfig: /etc/ovpn/openvpn.conf
